{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main glossa-morpho window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
